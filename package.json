{
  "name": "ipfs-contracts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "export NODE_ENV=test && npm run build && jest",
    "build": "waffle waffle.json",
    "lint": "eslint '{src,test}/**/*.ts'",
    "lint:fix": "eslint --fix '{src,test}/**/*.ts'",
    "flatten": "waffle flatten"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ethereum-waffle": "^3.3.0",
    "jest": "^26.6.3",
    "jest-environment-jsdom": "^25.5.0"
  },
  "dependencies": {
    "@ethereum-waffle/jest": "^3.2.2",
    "@openzeppelin/contracts": "^4.1.0"
  }
}
